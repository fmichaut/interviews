// DO NOT TOUCH

import type { DatatableProps } from "./exercise/type";

const Datatable = <Q ,>(props: DatatableProps<Q>) => {
  // we don't care about the render :)
  return <></>;
};

export default Datatable;
